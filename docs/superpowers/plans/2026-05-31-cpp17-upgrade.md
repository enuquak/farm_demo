# C++17 升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 系统性地将项目代码从 C++14 风格升级到 C++17，提升可读性、安全性和性能

**Architecture:** 增量式模块优化，按 common → dbmgr → gate_server → game_server 的顺序逐步应用三种 C++17 特性：结构化绑定、std::optional、std::string_view

**Tech Stack:** C++17, spdlog, nlohmann/json, protobuf, libevent

---

## 文件结构

### 将修改的文件

**common 模块:**
- `scripts/server/common/include/message_parser.h` - pack() 函数签名
- `scripts/server/common/src/message_parser.cpp` - pack() 函数实现

**dbmgr 模块:**
- `scripts/server/dbmgr/src/dbmgr_server.cpp` - 结构化绑定 (1处)
- `scripts/server/dbmgr/src/data_manager.h` - std::string_view (Category A)
- `scripts/server/dbmgr/src/data_manager.cpp` - std::string_view 实现

**gate_server 模块:**
- `scripts/server/gate_server/src/gate_server.h` - std::optional 函数声明
- `scripts/server/gate_server/src/gate_server.cpp` - 结构化绑定 (2处) + std::optional 实现
- `scripts/server/gate_server/src/session_manager.h` - std::string_view
- `scripts/server/gate_server/src/session_manager.cpp` - 结构化绑定 (2处)
- `scripts/server/gate_server/src/game_connection.h` - std::string_view

**game_server 模块:**
- `scripts/server/game_server/src/crop_system.cpp` - 结构化绑定 (1处)
- `scripts/server/game_server/src/dbmgr_connection_manager.h` - std::optional + std::string_view
- `scripts/server/game_server/src/dbmgr_connection_manager.cpp` - 结构化绑定 (4处) + std::optional
- `scripts/server/game_server/src/drop_item_manager.h` - std::optional
- `scripts/server/game_server/src/drop_item_manager.cpp` - 结构化绑定 (1处) + std::optional
- `scripts/server/game_server/src/game_scene_manager.h` - std::optional + std::string_view
- `scripts/server/game_server/src/game_scene_manager.cpp` - 结构化绑定 (1处) + std::optional
- `scripts/server/game_server/src/game_server.h` - std::string_view
- `scripts/server/game_server/src/game_server.cpp` - 结构化绑定 (1处)
- `scripts/server/game_server/src/player_manager.h` - std::optional
- `scripts/server/game_server/src/player_manager.cpp` - 结构化绑定 (4处) + std::optional
- `scripts/server/game_server/src/item_effects.h` - std::string_view
- `scripts/server/game_server/src/item_effects.cpp` - std::string_view 实现
- `scripts/server/game_server/src/item_interaction_handler.h` - std::optional

---

## 阶段 1: common 模块

### Task 1: MessageParser::pack 改为 std::string_view

**Files:**
- Modify: `scripts/server/common/include/message_parser.h:33`
- Modify: `scripts/server/common/src/message_parser.cpp:72-73`

- [ ] **Step 1: 修改 header 声明**

```cpp
// message_parser.h:33
// 原来:
static std::vector<uint8_t> pack(uint32_t msg_id, const std::string& payload);

// 改为:
static std::vector<uint8_t> pack(uint32_t msg_id, std::string_view payload);
```

- [ ] **Step 2: 修改实现**

```cpp
// message_parser.cpp:72-73
// 原来:
std::vector<uint8_t> MessageParser::pack(uint32_t msg_id, const std::string& payload) {
    return pack(msg_id, reinterpret_cast<const uint8_t*>(payload.data()), payload.size());
}

// 改为:
std::vector<uint8_t> MessageParser::pack(uint32_t msg_id, std::string_view payload) {
    return pack(msg_id, reinterpret_cast<const uint8_t*>(payload.data()), payload.size());
}
```

- [ ] **Step 3: 添加 #include <string_view>**

在 `message_parser.h` 顶部添加:
```cpp
#include <string_view>
```

- [ ] **Step 4: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/dbmgr/build && cmake --build .
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 5: 提交**

```bash
git add scripts/server/common/include/message_parser.h scripts/server/common/src/message_parser.cpp
git commit -m "refactor: change MessageParser::pack to accept std::string_view"
```

---

## 阶段 2: dbmgr 模块

### Task 2: dbmgr_server.cpp 结构化绑定

**Files:**
- Modify: `scripts/server/dbmgr/src/dbmgr_server.cpp:223-236`

- [ ] **Step 1: 修改 check_heartbeat() 循环**

```cpp
// dbmgr_server.cpp:223-236
// 原来:
for (auto& kv : game_sessions_) {
    auto& session = kv.second;
    if (now - session->last_heartbeat() > DBMGR_HEARTBEAT_TIMEOUT) {
        SPDLOG_WARN("[DBMgr]Session fd={} heartbeat timeout", kv.first);
        timeout_fds.push_back(kv.first);
    }
}

// 改为:
for (auto& [fd, session] : game_sessions_) {
    if (now - session->last_heartbeat() > DBMGR_HEARTBEAT_TIMEOUT) {
        SPDLOG_WARN("[DBMgr]Session fd={} heartbeat timeout", fd);
        timeout_fds.push_back(fd);
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/dbmgr/build && cmake --build .
```

- [ ] **Step 3: 提交**

```bash
git add scripts/server/dbmgr/src/dbmgr_server.cpp
git commit -m "refactor(dbmgr): use structured bindings in check_heartbeat"
```

### Task 3: DataManager 改为 std::string_view (Category A)

**Files:**
- Modify: `scripts/server/dbmgr/src/data_manager.h:69,72`
- Modify: `scripts/server/dbmgr/src/data_manager.cpp:185-197`

- [ ] **Step 1: 修改 header 声明**

```cpp
// data_manager.h
// 原来:
std::string get_account_file_path(const std::string& account_id) const;
uint32_t hash_account_id(const std::string& account_id) const;

// 改为:
std::string get_account_file_path(std::string_view account_id) const;
uint32_t hash_account_id(std::string_view account_id) const;
```

- [ ] **Step 2: 修改实现**

```cpp
// data_manager.cpp
// 原来:
std::string DataManager::get_account_file_path(const std::string& account_id) const {
    return accounts_dir_ + "/" + account_id + ".json";
}

uint32_t DataManager::hash_account_id(const std::string& account_id) const {
    uint32_t hash = 0;
    for (char c : account_id) {
        hash = hash * 31 + static_cast<uint32_t>(c);
    }
    return hash % dbmgr_count_;
}

// 改为:
std::string DataManager::get_account_file_path(std::string_view account_id) const {
    return accounts_dir_ + "/" + std::string(account_id) + ".json";
}

uint32_t DataManager::hash_account_id(std::string_view account_id) const {
    uint32_t hash = 0;
    for (char c : account_id) {
        hash = hash * 31 + static_cast<uint32_t>(c);
    }
    return hash % dbmgr_count_;
}
```

- [ ] **Step 3: 添加 #include <string_view>**

在 `data_manager.h` 顶部添加:
```cpp
#include <string_view>
```

- [ ] **Step 4: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/dbmgr/build && cmake --build .
```

- [ ] **Step 5: 提交**

```bash
git add scripts/server/dbmgr/src/data_manager.h scripts/server/dbmgr/src/data_manager.cpp
git commit -m "refactor(dbmgr): use std::string_view in DataManager helper functions"
```

---

## 阶段 3: gate_server 模块

### Task 4: gate_server.cpp 结构化绑定

**Files:**
- Modify: `scripts/server/gate_server/src/gate_server.cpp:109-111,629-634`

- [ ] **Step 1: 修改 stop() 循环**

```cpp
// gate_server.cpp:109-111
// 原来:
for (auto& kv : game_conns_) {
    kv.second->disconnect();
}

// 改为:
for (auto& [server_id, conn] : game_conns_) {
    conn->disconnect();
}
```

- [ ] **Step 2: 修改 handle_admin_shutdown() 循环**

```cpp
// gate_server.cpp:629-634
// 原来:
for (auto& kv : game_conns_) {
    if (kv.second->is_identified()) {
        kv.second->send(MSG_ID_SHUTDOWN, forward_payload);
        SPDLOG_INFO("[Gate]Forwarded shutdown to Game Server {}", kv.first);
    }
}

// 改为:
for (auto& [server_id, conn] : game_conns_) {
    if (conn->is_identified()) {
        conn->send(MSG_ID_SHUTDOWN, forward_payload);
        SPDLOG_INFO("[Gate]Forwarded shutdown to Game Server {}", server_id);
    }
}
```

- [ ] **Step 3: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
```

- [ ] **Step 4: 提交**

```bash
git add scripts/server/gate_server/src/gate_server.cpp
git commit -m "refactor(gate): use structured bindings in gate_server.cpp"
```

### Task 5: session_manager.cpp 结构化绑定

**Files:**
- Modify: `scripts/server/gate_server/src/session_manager.cpp:48-50,70-73`

- [ ] **Step 1: 修改 get_all_sessions() 循环**

```cpp
// session_manager.cpp:48-50
// 原来:
for (auto& kv : sessions_) {
    result.push_back(kv.second);
}

// 改为:
for (auto& [fd, session] : sessions_) {
    result.push_back(session);
}
```

- [ ] **Step 2: 修改 find_by_account_id() 循环**

```cpp
// session_manager.cpp:70-73
// 原来:
for (auto& kv : sessions_) {
    if (kv.second->account_id() == account_id) {
        return kv.second;
    }
}

// 改为:
for (auto& [fd, session] : sessions_) {
    if (session->account_id() == account_id) {
        return session;
    }
}
```

- [ ] **Step 3: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
```

- [ ] **Step 4: 提交**

```bash
git add scripts/server/gate_server/src/session_manager.cpp
git commit -m "refactor(gate): use structured bindings in session_manager.cpp"
```

### Task 6: GateServer std::optional

**Files:**
- Modify: `scripts/server/gate_server/src/gate_server.h:78-79`
- Modify: `scripts/server/gate_server/src/gate_server.cpp:136-165`

- [ ] **Step 1: 添加 #include <optional>**

在 `gate_server.h` 顶部添加:
```cpp
#include <optional>
```

- [ ] **Step 2: 修改 header 声明**

```cpp
// gate_server.h:78-79
// 原来:
GameConnection* get_game_connection(uint32_t server_id);
GameConnection* get_any_game_connection();

// 改为:
std::optional<GameConnection*> get_game_connection(uint32_t server_id);
std::optional<GameConnection*> get_any_game_connection();
```

- [ ] **Step 3: 修改实现**

```cpp
// gate_server.cpp:136-142
// 原来:
GameConnection* GateServer::get_game_connection(uint32_t server_id) {
    auto it = game_conns_.find(server_id);
    if (it != game_conns_.end() && it->second->is_identified()) {
        return it->second.get();
    }
    return nullptr;
}

// 改为:
std::optional<GameConnection*> GateServer::get_game_connection(uint32_t server_id) {
    auto it = game_conns_.find(server_id);
    if (it != game_conns_.end() && it->second->is_identified()) {
        return it->second.get();
    }
    return std::nullopt;
}
```

```cpp
// gate_server.cpp:144-165
// 原来:
GameConnection* GateServer::get_any_game_connection() {
    if (game_conns_.empty()) {
        return nullptr;
    }
    // ... round-robin logic ...
    return nullptr;
}

// 改为:
std::optional<GameConnection*> GateServer::get_any_game_connection() {
    if (game_conns_.empty()) {
        return std::nullopt;
    }
    // ... round-robin logic ...
    return std::nullopt;
}
```

- [ ] **Step 4: 更新调用处**

搜索所有调用 `get_game_connection()` 和 `get_any_game_connection()` 的地方，将 `if (conn)` 改为 `if (conn.has_value())` 或 `if (conn)`。

- [ ] **Step 5: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
```

- [ ] **Step 6: 提交**

```bash
git add scripts/server/gate_server/src/gate_server.h scripts/server/gate_server/src/gate_server.cpp
git commit -m "refactor(gate): use std::optional for nullable connection getters"
```

### Task 7: SessionManager::find_by_account_id 改为 std::string_view

**Files:**
- Modify: `scripts/server/gate_server/src/session_manager.h:28`
- Modify: `scripts/server/gate_server/src/session_manager.cpp:68-76`

- [ ] **Step 1: 修改 header 声明**

```cpp
// session_manager.h:28
// 原来:
std::shared_ptr<Session> find_by_account_id(const std::string& account_id) const;

// 改为:
std::shared_ptr<Session> find_by_account_id(std::string_view account_id) const;
```

- [ ] **Step 2: 修改实现**

```cpp
// session_manager.cpp:68-76
// 原来:
std::shared_ptr<Session> SessionManager::find_by_account_id(const std::string& account_id) const {
    for (auto& kv : sessions_) {
        if (kv.second->account_id() == account_id) {
            return kv.second;
        }
    }
    return nullptr;
}

// 改为 (已经使用结构化绑定):
std::shared_ptr<Session> SessionManager::find_by_account_id(std::string_view account_id) const {
    for (auto& [fd, session] : sessions_) {
        if (session->account_id() == account_id) {
            return session;
        }
    }
    return nullptr;
}
```

- [ ] **Step 3: 添加 #include <string_view>**

在 `session_manager.h` 顶部添加:
```cpp
#include <string_view>
```

- [ ] **Step 4: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
```

- [ ] **Step 5: 提交**

```bash
git add scripts/server/gate_server/src/session_manager.h scripts/server/gate_server/src/session_manager.cpp
git commit -m "refactor(gate): use std::string_view in find_by_account_id"
```

### Task 8: GameConnection::send 改为 std::string_view

**Files:**
- Modify: `scripts/server/gate_server/src/game_connection.h:34`
- Modify: `scripts/server/gate_server/src/game_connection.cpp:95`

- [ ] **Step 1: 修改 header 声明**

```cpp
// game_connection.h:34
// 原来:
bool send(uint32_t msg_id, const std::string& payload);

// 改为:
bool send(uint32_t msg_id, std::string_view payload);
```

- [ ] **Step 2: 修改实现**

```cpp
// game_connection.cpp:95
// 原来:
bool GameConnection::send(uint32_t msg_id, const std::string& payload) {
    return send(msg_id, reinterpret_cast<const uint8_t*>(payload.data()), payload.size());
}

// 改为:
bool GameConnection::send(uint32_t msg_id, std::string_view payload) {
    return send(msg_id, reinterpret_cast<const uint8_t*>(payload.data()), payload.size());
}
```

- [ ] **Step 3: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
```

- [ ] **Step 4: 提交**

```bash
git add scripts/server/gate_server/src/game_connection.h scripts/server/gate_server/src/game_connection.cpp
git commit -m "refactor(gate): use std::string_view in GameConnection::send"
```

---

## 阶段 4: game_server 模块

### Task 9: game_server.cpp 结构化绑定

**Files:**
- Modify: `scripts/server/game_server/src/game_server.cpp:305-313`

- [ ] **Step 1: 修改 check_heartbeat() 循环**

```cpp
// game_server.cpp:305-313
// 原来:
for (auto& kv : gate_sessions_) {
    auto& session = kv.second;
    if (now - session->last_heartbeat() > GATE_HEARTBEAT_TIMEOUT) {
        SPDLOG_WARN("[Game]Gate session fd={} heartbeat timeout", kv.first);
        timeout_fds.push_back(kv.first);
    }
}

// 改为:
for (auto& [fd, session] : gate_sessions_) {
    if (now - session->last_heartbeat() > GATE_HEARTBEAT_TIMEOUT) {
        SPDLOG_WARN("[Game]Gate session fd={} heartbeat timeout", fd);
        timeout_fds.push_back(fd);
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 3: 提交**

```bash
git add scripts/server/game_server/src/game_server.cpp
git commit -m "refactor(game): use structured bindings in check_heartbeat"
```

### Task 10: crop_system.cpp 结构化绑定

**Files:**
- Modify: `scripts/server/game_server/src/crop_system.cpp:127-135`

- [ ] **Step 1: 修改 serialize() 循环**

```cpp
// crop_system.cpp:127-135
// 原来:
for (const auto& kv : growing_tiles_) {
    auto [x, y] = parse_key(kv.first);
    nlohmann::json tile_data;
    tile_data["planted_at"] = kv.second.planted_at;
    tile_data["grow_time"] = kv.second.grow_time;
    tiles[make_string_key(x, y)] = tile_data;
}

// 改为:
for (const auto& [tile_key, crop] : growing_tiles_) {
    auto [x, y] = parse_key(tile_key);
    nlohmann::json tile_data;
    tile_data["planted_at"] = crop.planted_at;
    tile_data["grow_time"] = crop.grow_time;
    tiles[make_string_key(x, y)] = tile_data;
}
```

- [ ] **Step 2: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 3: 提交**

```bash
git add scripts/server/game_server/src/crop_system.cpp
git commit -m "refactor(game): use structured bindings in crop_system serialize"
```

### Task 11: drop_item_manager 结构化绑定 + std::optional

**Files:**
- Modify: `scripts/server/game_server/src/drop_item_manager.h:84`
- Modify: `scripts/server/game_server/src/drop_item_manager.cpp:64-71,80-86`

- [ ] **Step 1: 添加 #include <optional>**

在 `drop_item_manager.h` 顶部添加:
```cpp
#include <optional>
```

- [ ] **Step 2: 修改 header 声明**

```cpp
// drop_item_manager.h:84
// 原来:
const DropItem* get(uint32_t drop_id) const;

// 改为:
std::optional<const DropItem*> get(uint32_t drop_id) const;
```

- [ ] **Step 3: 修改 find_nearby() 循环**

```cpp
// drop_item_manager.cpp:64-71
// 原来:
for (const auto& kv : drops_) {
    float dx = kv.second.x - player_x;
    float dy = kv.second.y - player_y;
    float dist = std::sqrt(dx * dx + dy * dy);
    if (dist <= AUTO_PICKUP_DISTANCE) {
        nearby.push_back(kv.first);
    }
}

// 改为:
for (const auto& [drop_id, item] : drops_) {
    float dx = item.x - player_x;
    float dy = item.y - player_y;
    float dist = std::sqrt(dx * dx + dy * dy);
    if (dist <= AUTO_PICKUP_DISTANCE) {
        nearby.push_back(drop_id);
    }
}
```

- [ ] **Step 4: 修改 get() 实现**

```cpp
// drop_item_manager.cpp:80-86
// 原来:
const DropItem* DropItemManager::get(uint32_t drop_id) const {
    auto it = drops_.find(drop_id);
    if (it != drops_.end()) {
        return &it->second;
    }
    return nullptr;
}

// 改为:
std::optional<const DropItem*> DropItemManager::get(uint32_t drop_id) const {
    auto it = drops_.find(drop_id);
    if (it != drops_.end()) {
        return &it->second;
    }
    return std::nullopt;
}
```

- [ ] **Step 5: 更新调用处**

搜索所有调用 `get()` 的地方，将 `if (item)` 改为 `if (item.has_value())` 或 `if (item)`。

- [ ] **Step 6: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 7: 提交**

```bash
git add scripts/server/game_server/src/drop_item_manager.h scripts/server/game_server/src/drop_item_manager.cpp
git commit -m "refactor(game): use structured bindings and std::optional in drop_item_manager"
```

### Task 12: player_manager 结构化绑定 + std::optional

**Files:**
- Modify: `scripts/server/game_server/src/player_manager.h:39`
- Modify: `scripts/server/game_server/src/player_manager.cpp:105-111,116-118,124-127,137-140,289-291`

- [ ] **Step 1: 添加 #include <optional>**

在 `player_manager.h` 顶部添加:
```cpp
#include <optional>
```

- [ ] **Step 2: 修改 header 声明**

```cpp
// player_manager.h:39
// 原来:
Player* get_player(uint64_t player_id) const;

// 改为:
std::optional<Player*> get_player(uint64_t player_id) const;
```

- [ ] **Step 3: 修改 get_player() 实现**

```cpp
// player_manager.cpp:105-111
// 原来:
Player* PlayerManager::get_player(uint64_t player_id) const {
    auto it = players_.find(player_id);
    if (it != players_.end()) {
        return it->second.get();
    }
    return nullptr;
}

// 改为:
std::optional<Player*> PlayerManager::get_player(uint64_t player_id) const {
    auto it = players_.find(player_id);
    if (it != players_.end()) {
        return it->second.get();
    }
    return std::nullopt;
}
```

- [ ] **Step 4: 修改 get_all_players() 循环**

```cpp
// player_manager.cpp:116-118
// 原来:
for (auto& kv : players_) {
    result.push_back(kv.second.get());
}

// 改为:
for (auto& [player_id, player] : players_) {
    result.push_back(player.get());
}
```

- [ ] **Step 5: 修改 remove_players_by_gate() 循环**

```cpp
// player_manager.cpp:124-127
// 原来:
for (auto& kv : players_) {
    if (kv.second->gate_session() == gate_session) {
        to_remove.push_back(kv.first);
    }
}

// 改为:
for (auto& [player_id, player] : players_) {
    if (player->gate_session() == gate_session) {
        to_remove.push_back(player_id);
    }
}
```

- [ ] **Step 6: 修改 remove_players_by_gate_with_save() 循环**

```cpp
// player_manager.cpp:137-140
// 原来:
for (auto& kv : players_) {
    if (kv.second->gate_session() == gate_session) {
        to_remove.push_back(kv.first);
    }
}

// 改为:
for (auto& [player_id, player] : players_) {
    if (player->gate_session() == gate_session) {
        to_remove.push_back(player_id);
    }
}
```

- [ ] **Step 7: 修改 save_all_players() 循环**

```cpp
// player_manager.cpp:289-291
// 原来:
for (auto& kv : players_) {
    save_player_data(kv.first);
}

// 改为:
for (auto& [player_id, player] : players_) {
    save_player_data(player_id);
}
```

- [ ] **Step 8: 更新调用处**

搜索所有调用 `get_player()` 的地方，将 `if (player)` 改为 `if (player.has_value())` 或 `if (player)`。

- [ ] **Step 9: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 10: 提交**

```bash
git add scripts/server/game_server/src/player_manager.h scripts/server/game_server/src/player_manager.cpp
git commit -m "refactor(game): use structured bindings and std::optional in player_manager"
```

### Task 13: dbmgr_connection_manager 结构化绑定 + std::optional

**Files:**
- Modify: `scripts/server/game_server/src/dbmgr_connection_manager.h:153`
- Modify: `scripts/server/game_server/src/dbmgr_connection_manager.cpp:77-81,350-354,390-394,638-644,692-698`

- [ ] **Step 1: 添加 #include <optional>**

在 `dbmgr_connection_manager.h` 顶部添加:
```cpp
#include <optional>
```

- [ ] **Step 2: 修改 header 声明**

```cpp
// dbmgr_connection_manager.h:153
// 原来:
DBMgrConnection* find_connection_by_bev(struct bufferevent* bev);

// 改为:
std::optional<DBMgrConnection*> find_connection_by_bev(struct bufferevent* bev);
```

- [ ] **Step 3: 修改 shutdown() 循环**

```cpp
// dbmgr_connection_manager.cpp:77-81
// 原来:
for (auto& kv : pending_requests_) {
    if (kv.second.callback) {
        kv.second.callback(-1, nullptr, 0);
    }
}

// 改为:
for (auto& [request_id, request] : pending_requests_) {
    if (request.callback) {
        request.callback(-1, nullptr, 0);
    }
}
```

- [ ] **Step 4: 修改 handle_account_query_resp() 循环**

```cpp
// dbmgr_connection_manager.cpp:350-354
// 原来:
for (auto& kv : pending_account_requests_) {
    if (kv.second.dbmgr_index == conn->config_index()) {
        request_id = kv.first;
        break;
    }
}

// 改为:
for (auto& [req_id, request] : pending_account_requests_) {
    if (request.dbmgr_index == conn->config_index()) {
        request_id = req_id;
        break;
    }
}
```

- [ ] **Step 5: 修改 handle_account_set_resp() 循环**

```cpp
// dbmgr_connection_manager.cpp:390-394
// 原来:
for (auto& kv : pending_account_requests_) {
    if (kv.second.dbmgr_index == conn->config_index()) {
        request_id = kv.first;
        break;
    }
}

// 改为:
for (auto& [req_id, request] : pending_account_requests_) {
    if (request.dbmgr_index == conn->config_index()) {
        request_id = req_id;
        break;
    }
}
```

- [ ] **Step 6: 修改 fail_pending_requests_for() 循环**

```cpp
// dbmgr_connection_manager.cpp:638-644
// 原来:
for (auto& kv : pending_requests_) {
    if (kv.second.dbmgr_index == dbmgr_index) {
        if (kv.second.callback) {
            kv.second.callback(-1, nullptr, 0);
        }
        to_remove.push_back(kv.first);
    }
}

// 改为:
for (auto& [request_id, request] : pending_requests_) {
    if (request.dbmgr_index == dbmgr_index) {
        if (request.callback) {
            request.callback(-1, nullptr, 0);
        }
        to_remove.push_back(request_id);
    }
}
```

- [ ] **Step 7: 修改 find_connection_by_bev() 实现**

```cpp
// dbmgr_connection_manager.cpp:692-698
// 原来:
DBMgrConnection* DBMgrConnectionManager::find_connection_by_bev(struct bufferevent* bev) {
    auto it = bev_to_index_.find(bev);
    if (it == bev_to_index_.end()) return nullptr;
    uint32_t idx = it->second;
    if (idx >= connections_.size() || !connections_[idx]) return nullptr;
    return connections_[idx].get();
}

// 改为:
std::optional<DBMgrConnection*> DBMgrConnectionManager::find_connection_by_bev(struct bufferevent* bev) {
    auto it = bev_to_index_.find(bev);
    if (it == bev_to_index_.end()) return std::nullopt;
    uint32_t idx = it->second;
    if (idx >= connections_.size() || !connections_[idx]) return std::nullopt;
    return connections_[idx].get();
}
```

- [ ] **Step 8: 更新调用处**

搜索所有调用 `find_connection_by_bev()` 的地方，将 `if (conn)` 改为 `if (conn.has_value())` 或 `if (conn)`。

- [ ] **Step 9: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 10: 提交**

```bash
git add scripts/server/game_server/src/dbmgr_connection_manager.h scripts/server/game_server/src/dbmgr_connection_manager.cpp
git commit -m "refactor(game): use structured bindings and std::optional in dbmgr_connection_manager"
```

### Task 14: game_scene_manager 结构化绑定 + std::optional

**Files:**
- Modify: `scripts/server/game_server/src/game_scene_manager.h:25`
- Modify: `scripts/server/game_server/src/game_scene_manager.cpp:27-33,170-172`

- [ ] **Step 1: 添加 #include <optional>**

在 `game_scene_manager.h` 顶部添加:
```cpp
#include <optional>
```

- [ ] **Step 2: 修改 header 声明**

```cpp
// game_scene_manager.h:25
// 原来:
SceneState* get_scene(const std::string& scene_id) const;

// 改为:
std::optional<SceneState*> get_scene(const std::string& scene_id) const;
```

- [ ] **Step 3: 修改 get_scene() 实现**

```cpp
// game_scene_manager.cpp:27-33
// 原来:
SceneState* GameSceneManager::get_scene(const std::string& scene_id) const {
    auto it = scenes_.find(scene_id);
    if (it != scenes_.end()) {
        return it->second.get();
    }
    return nullptr;
}

// 改为:
std::optional<SceneState*> GameSceneManager::get_scene(const std::string& scene_id) const {
    auto it = scenes_.find(scene_id);
    if (it != scenes_.end()) {
        return it->second.get();
    }
    return std::nullopt;
}
```

- [ ] **Step 4: 修改 save_all_scenes() 循环**

```cpp
// game_scene_manager.cpp:170-172
// 原来:
for (auto& kv : scenes_) {
    save_scene_data(kv.first);
}

// 改为:
for (auto& [scene_id, scene] : scenes_) {
    save_scene_data(scene_id);
}
```

- [ ] **Step 5: 更新调用处**

搜索所有调用 `get_scene()` 的地方，将 `if (scene)` 改为 `if (scene.has_value())` 或 `if (scene)`。

- [ ] **Step 6: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 7: 提交**

```bash
git add scripts/server/game_server/src/game_scene_manager.h scripts/server/game_server/src/game_scene_manager.cpp
git commit -m "refactor(game): use structured bindings and std::optional in game_scene_manager"
```

### Task 15: item_effects + item_interaction_handler std::optional + std::string_view

**Files:**
- Modify: `scripts/server/game_server/src/item_effects.h:96-98,105-106`
- Modify: `scripts/server/game_server/src/item_effects.cpp:93-128,164-185`
- Modify: `scripts/server/game_server/src/item_interaction_handler.h:56-68`

- [ ] **Step 1: 添加 #include <optional> 和 #include <string_view>**

在 `item_effects.h` 和 `item_interaction_handler.h` 顶部添加:
```cpp
#include <optional>
#include <string_view>
```

- [ ] **Step 2: 修改 ItemEffects header 声明**

```cpp
// item_effects.h:96-98,105-106
// 原来:
static const ItemEffect* get_effect(int32_t item_id,
                                     const std::string& obj_type,
                                     const std::string& ground_type);
static int string_to_ground_type(const std::string& name);
static int string_to_object_type(const std::string& name);

// 改为:
static std::optional<const ItemEffect*> get_effect(int32_t item_id,
                                                     std::string_view obj_type,
                                                     std::string_view ground_type);
static int string_to_ground_type(std::string_view name);
static int string_to_object_type(std::string_view name);
```

- [ ] **Step 3: 修改 get_effect() 实现**

```cpp
// item_effects.cpp:93-128
// 原来:
const ItemEffect* ItemEffects::get_effect(int32_t item_id, const std::string& obj_type, const std::string& ground_type) {
    auto& all_effects = get_effects();
    auto it = all_effects.find(item_id);
    if (it == all_effects.end()) {
        return nullptr;
    }
    // ... priority matching logic ...
    return nullptr;
}

// 改为:
std::optional<const ItemEffect*> ItemEffects::get_effect(int32_t item_id, std::string_view obj_type, std::string_view ground_type) {
    auto& all_effects = get_effects();
    auto it = all_effects.find(item_id);
    if (it == all_effects.end()) {
        return std::nullopt;
    }
    // ... priority matching logic ...
    return std::nullopt;
}
```

注意：在优先级匹配逻辑中，需要将 `std::string key = "obj:" + obj_type;` 改为 `std::string key = std::string("obj:").append(obj_type);`

- [ ] **Step 4: 修改 string_to_ground_type() 和 string_to_object_type() 实现**

```cpp
// item_effects.cpp:164-185
// 原来:
int ItemEffects::string_to_ground_type(const std::string& name) {
    if (name == "GRASS") return 0;
    // ...
}

// 改为:
int ItemEffects::string_to_ground_type(std::string_view name) {
    if (name == "GRASS") return 0;
    // ...
}
```

- [ ] **Step 5: 修改 PlayerInventory::get_active_slot() 实现**

```cpp
// item_interaction_handler.h:56-68
// 原来:
InventorySlot* get_active_slot() {
    if (active_slot >= 0 && active_slot < 30) {
        return &slots[active_slot];
    }
    return nullptr;
}

const InventorySlot* get_active_slot() const {
    if (active_slot >= 0 && active_slot < 30) {
        return &slots[active_slot];
    }
    return nullptr;
}

// 改为:
std::optional<InventorySlot*> get_active_slot() {
    if (active_slot >= 0 && active_slot < 30) {
        return &slots[active_slot];
    }
    return std::nullopt;
}

std::optional<const InventorySlot*> get_active_slot() const {
    if (active_slot >= 0 && active_slot < 30) {
        return &slots[active_slot];
    }
    return std::nullopt;
}
```

- [ ] **Step 6: 更新调用处**

搜索所有调用这些函数的地方，更新空值检查逻辑。

- [ ] **Step 7: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 8: 提交**

```bash
git add scripts/server/game_server/src/item_effects.h scripts/server/game_server/src/item_effects.cpp scripts/server/game_server/src/item_interaction_handler.h
git commit -m "refactor(game): use std::optional and std::string_view in item_effects and item_interaction_handler"
```

### Task 16: game_server 相关 std::string_view 改进 (Category B)

**Files:**
- Modify: `scripts/server/game_server/src/game_server.h:85,94`
- Modify: `scripts/server/game_server/src/game_server.cpp:598,781`
- Modify: `scripts/server/game_server/src/game_scene_manager.h:33`
- Modify: `scripts/server/game_server/src/game_scene_manager.cpp:165`
- Modify: `scripts/server/game_server/src/dbmgr_connection_manager.h:108,150`
- Modify: `scripts/server/game_server/src/dbmgr_connection_manager.cpp:688,700-708`

- [ ] **Step 1: 修改 GameServer header 声明**

```cpp
// game_server.h:85,94
// 原来:
void handle_enter_game_req(std::shared_ptr<GateSession> session, uint64_t player_id, const std::string& payload);
void send_to_gate(std::shared_ptr<GateSession> session, uint32_t msg_id, const std::string& payload);

// 改为:
void handle_enter_game_req(std::shared_ptr<GateSession> session, uint64_t player_id, std::string_view payload);
void send_to_gate(std::shared_ptr<GateSession> session, uint32_t msg_id, std::string_view payload);
```

- [ ] **Step 2: 修改 GameServer 实现**

```cpp
// game_server.cpp:598,781
// 修改函数签名和实现，保持逻辑不变
```

- [ ] **Step 3: 修改 GameSceneManager header 声明**

```cpp
// game_scene_manager.h:33
// 原来:
static std::string make_scene_data_key(const std::string& scene_id);

// 改为:
static std::string make_scene_data_key(std::string_view scene_id);
```

- [ ] **Step 4: 修改 GameSceneManager 实现**

```cpp
// game_scene_manager.cpp:165
// 原来:
return "scene:" + scene_id;

// 改为:
return std::string("scene:").append(scene_id);
```

- [ ] **Step 5: 修改 DBMgrConnectionManager header 声明**

```cpp
// dbmgr_connection_manager.h:108,150
// 原来:
void broadcast_message(uint32_t msg_id, const std::string& payload);
void send_to_dbmgr(DBMgrConnection* conn, uint32_t msg_id, const std::string& payload);

// 改为:
void broadcast_message(uint32_t msg_id, std::string_view payload);
void send_to_dbmgr(DBMgrConnection* conn, uint32_t msg_id, std::string_view payload);
```

- [ ] **Step 6: 修改 DBMgrConnectionManager 实现**

```cpp
// dbmgr_connection_manager.cpp:688,700-708
// 修改函数签名和实现，保持逻辑不变
```

- [ ] **Step 7: 编译验证**

```bash
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .
```

- [ ] **Step 8: 提交**

```bash
git add scripts/server/game_server/src/game_server.h scripts/server/game_server/src/game_server.cpp scripts/server/game_server/src/game_scene_manager.h scripts/server/game_server/src/game_scene_manager.cpp scripts/server/game_server/src/dbmgr_connection_manager.h scripts/server/game_server/src/dbmgr_connection_manager.cpp
git commit -m "refactor(game): use std::string_view in send functions and key generation"
```

---

## 验证步骤

每个阶段完成后，执行以下验证：

1. **编译验证**：确保所有三个模块都能成功编译
2. **测试验证**：运行现有测试（如果有）
3. **功能验证**：启动服务，检查日志，确保基本功能正常

```bash
# 编译所有模块
cd D:/mb_workspace/farm_demo/scripts/server/dbmgr/build && cmake --build .
cd D:/mb_workspace/farm_demo/scripts/server/gate_server/build && cmake --build .
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && cmake --build .

# 运行测试（如果有）
cd D:/mb_workspace/farm_demo/scripts/server/game_server/build && ctest
```
